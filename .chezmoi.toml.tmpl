{{- $name := promptStringOnce . "name" "Name" -}}
{{- $email := promptStringOnce . "email" "Email" -}}
{{- $hypr := promptBoolOnce . "hypr" "Install and setup hypr" false -}}
{{- $rust := promptBoolOnce . "rust" "Install and setup rust" true -}}

[data]
  name = {{ $name | quote }}
  email = {{ $email | quote }}
  hypr = {{ $hypr }}
  rust = {{ $rust }}
